/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'thumbs.dreamstime.com',
      'wger.de',
      'wger.denull'
    ],
  }
}

module.exports = nextConfig
